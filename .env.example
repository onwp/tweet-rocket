# Anthropic API Key (required for AI tweet generation)
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Set to "true" to skip the client-side API key input
# When ANTHROPIC_API_KEY is set, users don't need to provide their own key
